services:
  db:
    image: postgres:18-alpine
    restart: always
    shm_size: ${POSTGRES_SHM_SIZE:-512m}
    entrypoint: ["/bin/sh", "/usr/local/bin/1flowbase-postgres-entrypoint.sh"]
    command: ["postgres", "-c", "max_connections=${POSTGRES_MAX_CONNECTIONS:-300}"]
    env_file:
      - ./middleware.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-1flowbase}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-1flowbase}
    ports:
      - "${POSTGRES_PORT:-35432}:5432"
    volumes:
      - ${POSTGRES_DATA_DIR:-./volumes/postgres}:/var/lib/postgresql
      - ./postgres/entrypoint.sh:/usr/local/bin/1flowbase-postgres-entrypoint.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-1flowbase}"]
      interval: 5s
      timeout: 3s
      retries: 20
