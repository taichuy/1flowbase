# 1flowbase UI 设计规范

## 1. 文档目标与真值

本规范只服务当前 1flowbase 仓库，用于回答四个问题：页面属于哪种视觉层、颜色表达什么、优先使用哪个共享组件、允许多强的视觉装饰。

### 1.1 视觉定义

1flowbase 是一个以翡翠绿为品牌信号、以薄荷暖白为环境底色、以白色结构面承载高密度工作内容的 AI 应用平台。

- 品牌入口允许柔光、渐变、半透明和克制的动态氛围。
- 后台工作区继承品牌色调，但降低装饰强度，优先保证结构、密度和操作清晰度。
- 编辑器与设计工具使用更直接的蓝色操作反馈，避免与品牌绿和运行状态混淆。
- 所有层级都保持明亮、清晰、工程化；不发展成深色仪表盘或彩色卡片墙。

### 1.2 真值优先级

```text
产品语义与本规范
  ↓ 定义颜色角色、层级、组件选择和页面语法
主题与 token
  ↓ 提供精确实现值
共享组件
  ↓ 提供稳定结构与交互
Feature 局部样式
  ↓ 只处理最近 owner 的特殊场景
```

精确实现值以当前代码为准：

- CSS 变量：`web/app/src/styles/tokens.css`
- Ant Design 主题：`web/packages/ui/src/theme.ts`
- 应用级基线：`web/app/src/styles/globals.css`
- 后台共享组件：`web/app/src/shared/ui/`
- 壳层：`web/app/src/app-shell/`
- 品牌入口参考：`web/app/src/features/auth/`
- 编辑器：`web/app/src/features/agent-flow/components/editor/`

不得从业务页面偶然出现的硬编码颜色反推全局规范。需要新增全局视觉角色时，先进入主题或 token；只服务单一场景时，留在最近 owner。

---

## 2. 三种视觉表达层

### 2.1 Brand Layer：品牌入口

适用：登录页、品牌展示区、少量引导型空状态。

视觉语法：

- 翡翠绿是主信号，薄荷白是环境底色。
- 极淡青蓝只作为环境光和空间层次，不与翡翠绿争夺主导权。
- 允许点阵、粒子、柔和径向光、品牌文字渐变和轻玻璃表面。
- 动效必须慢、轻、低干扰；表单和主要操作始终保持清楚可读。
- Brand Layer 的装饰语言不得整体复制到后台内容区。

登录页是当前 Brand Layer 的基准，不是所有页面的布局模板。

### 2.2 Product Layer：后台工作区

适用：应用管理、设置、数据管理、日志、API、监控、个人中心等 Shell 页面。

视觉语法：

- 使用暖白浅绿页面基底、白色或近白结构面、低饱和暖灰绿边框。
- 依靠表面层差、边框和轻阴影组织信息，不依靠大面积彩色填充。
- 导航、筛选、表格、表单、Descriptions、Drawer 和 Modal 优先复用 Ant Design 与 `shared/ui`。
- 管理页面优先使用纵向结构和数据表格，不使用 Card 套 Card 或卡片墙。
- 单行状态、轻量工具条和摘要不单独包成高白大卡片。

### 2.3 Tool Layer：编辑与操纵界面

适用：Agent Flow、Frontstage 设计模式、画布、Inspector、数据行操作选择。

视觉语法：

- 密度高于 Product Layer，结构线更明确，装饰更少。
- 蓝色表达用户正在编辑、操纵或选中的对象。
- 品牌绿仍可用于全局主操作、焦点和运行信号，但不替代蓝色编辑反馈。
- 画布使用 `xyflow + Editor UI` 自封装；不得把大段 Shell 表单直接塞入节点主体。
- Shell 与 Tool Layer 共享排版、基础表面、状态语义和控件质量，不发展成两个产品。

---

## 3. 语义颜色系统

### 3.1 颜色关系

```text
翡翠绿 = 品牌、当前入口、主操作倾向、焦点、运行信号
薄荷绿 = 品牌浅表面、导航激活、低强度强调
蓝色   = 编辑、操纵、画布选择、数据行选择
状态色 = 系统正在发生的事实
中性色 = 页面结构、文字层级和默认控件
```

颜色必须先有语义角色，再进入组件。不得仅为了“丰富”增加彩色文字、彩色卡片或彩色标签。

### 3.2 品牌与交互色

| 角色 | 当前基准 | 主要用途 | 不应用于 |
| --- | --- | --- | --- |
| `brand-primary` | `#00ab73` | 品牌锚点、主操作、焦点、活动导航指示 | 大面积实色背景 |
| `brand-strong` | `#00c283` | hover、品牌渐变亮部、少量高信号元素 | 小号正文 |
| `brand-text` | `#008f5f` | 浅色按钮文字、需要可读性的绿色文本 | 弱提示文字 |
| `brand-mint` | `#e6f7f2` | 导航激活、品牌浅表面、轻选中 | 运行状态判断 |
| `interaction-blue` | `#1677ff` | 画布操纵、设计模式、数据行选择 | 品牌主 CTA、成功状态 |
| `info-teal` | `#2bb9b1` | 信息提示、关联高亮、辅助选中反馈 | 代替所有 selected 状态 |

`#00d084` 可作为品牌亮绿使用，但不应直接用于小字号正文；需要文本对比度时使用 `brand-text` 或主题计算后的可读色。

### 3.3 表面、文字与结构

| 角色 | 当前基准 | 用途 |
| --- | --- | --- |
| `page-bg` | `#f4f8f5` | 后台环境底色 |
| `surface` | `#ffffff` | 表格、表单、Drawer、主要内容面 |
| `surface-raised` | `#f8fcf9` / `#fcfffd` | 表头、折叠标题、抬升区域 |
| `text-primary` | `#16211d` | 标题、正文、关键数据 |
| `text-secondary` | `#55645d` | 标签、导航默认态、说明 |
| `text-tertiary` | `#7b8982` | hint、时间、辅助信息 |
| `border-default` | `#d5ddd8` | 容器和控件边界 |
| `border-subtle` | `#e8edea` | 表格、列表和区域内部结构线 |
| `code-bg` | `#0d1713` | 需要高对比的代码或日志表面 |

原则：

- 页面基底与主要内容面至少保持一层可见层差。
- 内部结构线优先使用 `border-subtle`，不要让每一行都显得像独立卡片。
- 正文以中性文字为主；彩色文字只出现在交互、状态或品牌语义明确的地方。

### 3.4 系统状态色

| 状态 | 当前基准 | 语义 |
| --- | --- | --- |
| `running` | `#00ab73` | 系统正在执行 |
| `waiting` | `#ffba00` | 等待输入、队列或外部回调 |
| `failed` | `#fb565b` | 失败、阻塞、需要排查 |
| `success` | `#19b36b` | 已成功、健康、已完成 |
| `draft` | `#6b7280` | 草稿、尚未发布 |

状态规则：

- `running` 与 `success` 不可互换：前者表示过程，后者表示结果。
- 类型、分类、供应商和资源种类使用中性 Tag，不占用状态色。
- `published` 是发布语义，不等于 `running` 或 `success`。
- “选中”不是系统状态，按交互上下文使用薄荷绿、蓝色或信息青色。
- 同一运行状态在列表、NodeCard、Inspector 和日志详情中必须保持一致。

---

## 4. 排版、密度、形状与高程

### 4.1 字体与层级

默认 UI 字体遵循当前主题：

```css
font-family: "IBM Plex Sans", "Segoe UI", sans-serif;
```

代码与工程数据使用稳定等宽字体栈：

```css
font-family: "SFMono-Regular", Menlo, Monaco, Consolas, "Liberation Mono", monospace;
```

| 角色 | 建议规格 | 用途 |
| --- | --- | --- |
| Page title | `20px / 600` | 页面唯一主标题 |
| Dialog title | `16px / 600` | Modal、Drawer、大型面板 |
| Section title | `14px / 600` | 区块、Inspector、表格工具区 |
| Body | `14px / 400` | 默认正文和数据 |
| Label | `14px / 400–500` | 表单和字段标签 |
| Caption | `12px / 400` | hint、时间、辅助说明 |
| Code | `13px / 400` | 路径、代码、运行片段 |
| Metric | `24px / 600` | 少量关键指标 |

后台排版紧凑、清楚，不使用营销式大标题。Brand Layer 可使用响应式展示字号，但不能挤压主要表单或操作。

### 4.2 间距

基础节奏为 `4px`，常用序列为 `4 / 8 / 12 / 16 / 20 / 24 / 32px`。

- `4–8px`：图标、紧凑动作、Tag 内部。
- `12–16px`：字段、列表行、普通组件内部。
- `20–24px`：独立容器、筛选区、页面 section。
- `32px`：明显任务块之间。

间距表达信息关系，不为制造“高级感”堆积空白。

### 4.3 圆角层级

| 层级 | 建议范围 | 适用对象 |
| --- | --- | --- |
| Micro | `4px` | 状态标记、代码标签、紧凑 badge |
| Control | `8px` | Input、Button、Collapse、代码块、内部区域 |
| Surface | `12px` | Modal、Drawer 内容面、普通独立面板 |
| Page surface | `16–18px` | 筛选区、大型工作区、浮层菜单 |
| Pill | `999px` | 登录方式、明确胶囊按钮、少量状态标签 |

同一视觉层级保持一致，比强制全站只有三档数值更重要。不得在相邻同类组件中无理由混用圆角。

### 4.4 阴影与半透明

- 常规结构优先使用边框，无需阴影。
- 页面级独立容器可使用两层低强度阴影，表达从页面基底轻微抬升。
- Drawer、Popover、下拉菜单和浮动窗口使用 `shadow-float`。
- 半透明与 `backdrop-filter` 只用于顶栏、品牌入口和确实悬浮的表面。
- Brand Layer 允许柔光；Product Layer 不允许整页发光；Tool Layer 只对焦点或活动控制使用局部 halo。

---

## 5. 页面构造原则

### 5.1 标准页面骨架

```text
页面
  ├─ 必要标题与当前任务操作
  ├─ 可选筛选 / 摘要区
  ├─ 一个主内容结构
  └─ Drawer / Modal 承担临时深层任务
```

- 一个页面只回答一个主要任务域问题。
- 页面主块负责完成任务，辅助块只补充上下文。
- 高密度后台页面优先使用 Table、Descriptions、Form、Typography、Divider、Space/Flex。
- 不用多层 Card 包装布局；不要为每组字段创建一张卡。
- 同一页面避免出现多个同等级高阴影容器。

### 5.2 页面宽度与高度

- 普通后台页使用自然高度和清晰纵向 section。
- 需要持续滚动的数据工作区使用 viewport 模式，由主内容 owner 管理滚动。
- 需要专注的窄表单或说明内容可限制到约 `800px`。
- 表格、编辑器、API Explorer 等工作区允许占满可用宽度。

### 5.3 详情模型

工作区只有两种 L1 详情模型：

| 模型 | 场景 | 行为 |
| --- | --- | --- |
| Drawer | Shell 列表行、日志行、管理对象 | 模态或受控浮层；关闭后焦点回退 |
| Inspector | Canvas 节点、连线、编辑对象 | 非模态；原地更新；保留画布上下文 |

创建、确认、批量配置等必须阻断当前任务的操作可使用 Modal，但不能用 Modal 代替同类对象的常规详情路径。

---

## 6. 共享组件选择指南

优先复用当前共享组件。它们既是代码资产，也是已经确认的页面语法。

| 场景 | 优先组件 | 视觉责任 |
| --- | --- | --- |
| 设置或应用分区页 | `SectionPageLayout` | 侧栏、内容宽度、viewport 与移动降级 |
| 管理型数据列表 | `DataTable` | 滚动、固定表头、列宽、分页、列选择 |
| 查询条件 + 数据列表 | `DataTableLayout` | 筛选表面与表格主区的层级关系 |
| 对象属性摘要 | `Descriptions` | 标签和值的稳定对齐 |
| 简单对象集合 | Structured List pattern / `.structured-list` | 轻量结构行，不升级成卡片墙 |
| 可展开配置组 | `CollapseShell` | 结构边界、激活标题和密度变体 |
| 辅助详情或编辑 | `ResizableDrawer` | 可调整宽度的深层任务 |
| 阻断式复杂操作 | `FixedHeightModal` | 固定可用高度和内部滚动 |
| 长内容区域 | `ScrollableSurface` | 明确滚动 owner |
| JSON / 工程内容 | `JsonPreviewBlock` | 工程信息、折叠、复制与编辑承载 |
| 浮动工作区 | `WindowWorkspace` | 拖动、调整尺寸和边界控制 |
| Canvas 对象详情 | Editor Inspector / Panel | 保留画布上下文 |

只有出现新的稳定跨 feature 语法时才扩展 `shared/ui`。单一页面的特殊表现留在 feature 内。

---

## 7. 核心组件视觉规则

### 7.1 Button

- Primary：当前主题采用浅翡翠填充、深绿色文字和低强度绿色边框，不使用大面积高饱和实心绿。
- Default：近白表面、中性文字、暖灰绿边框；hover 通过边框和轻底色反馈。
- Link / Text：用于低层级导航和辅助动作，不与主按钮争夺注意力。
- Danger：只用于删除、停用等破坏性动作，使用红色语义。
- 按钮必须产生当前上下文可验证的结果；说明文字和未开放入口不得伪装成按钮。
- 同一操作组只保留一个最高层级动作。

### 7.2 Input、Select 与 Form

- 默认高度约 `32px`，复杂登录入口可使用更高的品牌化控件。
- 白色或近白背景，暖灰绿边框，品牌绿 focus ring。
- 表单按任务分组，不用 Card 为每个字段分组。
- 错误信息靠近字段；全局错误使用 Alert 或页面级反馈。

### 7.3 Table 与筛选区

- 管理型页面默认优先 Table，而不是卡片列表。
- 表头使用轻抬升面和次级文字；正文区保持白色或透明结构面。
- 筛选区可以作为 `16–18px` 页面级圆角表面，并使用克制双层阴影。
- 数据行选中使用低透明度操作蓝；hover 只增强一档。
- 工具条、分页和表格滚动各有明确 owner，不让整页出现多个滚动条。

### 7.4 Navigation

- 顶部壳层可使用半透明薄荷白与 blur，保持轻盈但不影响文字清晰度。
- 一级导航以文字和底部指示为主，不使用大块实色标签。
- Section 侧栏激活项使用薄荷绿底与品牌亮绿；编辑器内部导航可按 Tool Layer 使用蓝色。
- 导航文案、`route id`、`path`、选中态和权限 key 必须来自同一真值层。

### 7.5 Collapse、Descriptions 与 Structured List pattern

- 用结构线和轻表面组织信息，不把每一项做成独立浮卡。
- Collapse 激活标题允许低强度品牌渐变，内容仍回到白色结构面。
- Structured List 默认无外层高程；需要边界时只增加一层 border 和标准圆角。
- Descriptions 适合只读属性，不应承担复杂编辑表单。

### 7.6 Drawer、Modal 与 Popover

- Drawer 承担保持来源上下文的详情和编辑；宽度可按内容调整。
- Modal 只用于需要阻断、确认或一次性完成的任务。
- Popover 和下拉菜单允许轻半透明、blur 和浮层阴影。
- 所有浮层必须有清楚标题、关闭路径、焦点管理和可预测滚动区域。

### 7.7 Code、JSON 与日志

- 工程内容使用等宽字体和明确结构，不作为装饰。
- 短 JSON 预览可使用浅色填充表面；长代码、日志或终端内容可使用深色代码面。
- 原始异常对象和无解释 JSON 不直接暴露在默认产品 UI。

---

## 8. 常见页面 Recipe

| 页面 / Section | 主块 | 辅助块 |
| --- | --- | --- |
| 登录 | 品牌 Hero + 单一认证任务 | 登录方式选择、页脚来源 |
| Home | 应用目录、筛选、创建入口 | 编辑、标签管理、导入 |
| Application Orchestration | Canvas + Inspector + flow 状态 | 节点选择、版本、问题抽屉 |
| Application API | 发布契约、认证、请求响应结构 | 示例、版本、变更提示 |
| Application Logs | 筛选 + 运行列表 + Run Drawer | 时间范围、计数、导出 |
| Application Monitoring | 健康摘要 + 指标 / 图 + 异常列表 | 时间范围、阈值、刷新时间 |
| Settings | `SectionPageLayout` + 当前 section body | section 导航与必要说明 |
| Data Management | `DataTableLayout` + `DataTable` | Drawer / Modal 深层任务 |

Recipe 约束任务结构，不规定每个页面必须拥有相同像素布局。

---

## 9. Editor UI Layer

### 9.1 Canvas

- Canvas 是工作表面，不是品牌背景；使用中性或极浅网格，保持节点和连线可辨。
- 活动控制、拖拽、选中、对齐线和设计模式使用操作蓝。
- 运行中的节点和连线使用 `running` 绿；失败使用 `failed` 红；不要用蓝色冒充运行态。
- 空白画布允许提供引导，但不得使用营销式大段说明。

### 9.2 NodeCard

- 比 Shell 更紧凑，保持清楚标题、类型、关键配置摘要和状态。
- 节点类型使用中性图标或 Tag；状态使用统一状态语义。
- selected 与 running 可以同时存在：selected 表达用户操作，running 表达系统事实。
- 节点主体不直接堆入大段 Ant Design Form；复杂配置进入 Inspector。

### 9.3 Inspector 与工具面板

- Inspector 非模态，选择对象后原地更新。
- 面板分组优先使用结构标题、Divider、Collapse 和紧凑字段，不使用多层 Card。
- 可拖动、调整大小的工具面板必须有明确 handle、最小尺寸和 viewport 边界。

---

## 10. 响应式与可访问性

### 10.1 断点行为

- 桌面宽屏：保留完整壳层、侧栏、工作区和 Inspector。
- 窄桌面 / 平板：收紧间距，侧栏进入 Tabs 或 Drawer，避免多个并排辅助块。
- 移动端：主内容单列，Drawer 全宽，筛选表单单列。
- `max-width: 768px` 下不把桌面画布硬塞进手机；展示摘要和桌面操作提示。

### 10.2 首屏与触摸

- 移动首屏优先展示当前页标题、关键状态和不超过两个主动作。
- 主要触摸目标至少 `44 × 44px`；紧凑桌面图标按钮仍需可聚焦并有 Tooltip 或 aria-label。
- 交互不只依赖颜色；选中、错误和状态同时提供结构、图标或文字反馈。
- focus-visible 必须清晰，不能为了视觉简洁移除键盘焦点。

---

## 11. 样式实现边界

样式调整固定遵循：

```text
Theme Token
  → First-Party Wrapper
    → Explicit Third-Party Slot
      → Stop
```

- 全局统一颜色、字体、圆角、阴影和基础密度时，优先修改主题与 token。
- 页面和组件布局由最近的 first-party wrapper 负责。
- 只有明确知道 blast radius 时，才从自有 wrapper 命中一个第三方 slot。
- 禁止裸写全局 `.ant-*` 或依赖第三方递归内部 DOM 链。
- 禁止为局部视觉问题无边界修改第三方内部 `display / position / height / padding / gap / overflow`。
- Feature 私有样式跟随最近 owner；只有跨 feature 稳定复用后才上提。

---

## 12. Do / Don't

### Do

- 从页面所属表达层开始判断视觉强度。
- 使用翡翠绿建立品牌连续性，使用薄荷白建立环境层次。
- 使用蓝色表达编辑和操纵，使用状态色表达系统事实。
- 优先复用 Ant Design、主题 token 和 `shared/ui`。
- 用一个主内容结构组织页面，以 Drawer / Modal 承担深层任务。
- 用边框、表面差和低强度阴影建立层级。
- 让登录页保有品牌感，让后台工作区保持克制和高效。

### Don't

- 不把登录页的粒子、柔光和玻璃效果复制到所有后台页面。
- 不把所有选中态强行统一为同一种绿色。
- 不用状态色表达类型、品牌装饰或分类。
- 不把管理页面做成 Card 套 Card 或卡片墙。
- 不在同类组件中无理由混用圆角、阴影和密度。
- 不从单个业务页面的硬编码样式反推全局规则。
- 不为了实现一张参考图破坏现有共享组件和页面语法。
- 不在默认 UI 暴露提示词、内部指令、调试文本、原始异常对象或无解释 JSON。

---

## 13. 实现前快速判断

开始前回答：

1. 当前页面属于 Brand、Product 还是 Tool Layer？
2. 当前颜色表达品牌、交互、选中还是系统状态？
3. 是否已有 `shared/ui` 或 Ant Design 组件承担这个结构？
4. 主内容是什么，辅助内容是否反客为主？
5. 详情应该使用 Drawer、Inspector 还是阻断式 Modal？
6. 样式应落在 theme token、first-party wrapper 还是明确 slot？
7. 移动端是否做了诚实降级？

无法回答时先停止视觉抛光，回到页面任务和组件责任。
