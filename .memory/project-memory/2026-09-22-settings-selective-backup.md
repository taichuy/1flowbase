---
created_at: 2026-09-22 08
memory_type: project
decision_policy: verify_before_decision
scope: settings selective backup and data import
---

# 后台设置轻量备份

用户于 2026-09-22 确认按后台设置分类，每项选择「不备份 / 结构 / 结构和数据」，默认不备份；结构=配置记录，结构和数据=配置记录+对应全部持久化数据；不备份插件包，不重复建表，不替换整库。文件实体默认不带、另行勾选；原主键 upsert，目标独有记录保留。缺插件警告并确认后继续，缺表/不兼容阻止写入；旧备份恢复保持可用。动机是现有本地导出约 10GB，希望按需缩小范围，而非系统克隆。

用户授权在 worktree 完成、验证后合回 dev。Root 复用干净、已合并、无运行进程的 /home/taichuy/git/git_worktree/complexity-gate-review-20260911，切到 codex/settings-backup-selective-20260922；初始 SHA cb7533991。主工作树保持 dev，其他任务已推进它，集成时重新核对。

只读 Scout 发现 SettingsFeature 没有表归属映射，需有限显式清单；运行日志/trajectory 的完整依赖范围未闭合。用户已进一步确认：携带必要插件身份/引用记录，仍排除包、二进制、缓存、本机安装状态，不伪造运行可用性。用户进一步纠正：数据选中后完整导出对应设置数据，不再排除运行日志/调用轨迹或追加排除规则；接口尽量轻量。范围已闭合，继续实现。

当前源码集成验收已通过并合回、推送 dev；实际代码版本以 git 为准。结构独立导入会验证引用物理表和生成表达式；插件身份只携带逻辑字段，保留目标安装策略，新身份不声称已安装。恢复完成需重启服务加载设置。定向测试与证据见 tmp/test-governance/settings-backup/qa-source-integration.md；没有进行本地10GB性能基准，UI证据仅候选mock场景。无后续待批准范围或截止日期。

与 2026-08-22 备份工件清单方案不同：本次新轻量备份明确不携带插件工件，旧全量恢复保留兼容路径。

2026-09-22 10 实际跨环境验收：按用户确认导出数据建模/应用/UI三项结构，约718KiB，35表3184行，上传到1flowbase_latest的本地beta（已同步dev、3200/7900运行）。预检失败：6张自定义物理表缺失、用户/角色引用缺失、初始化内置模型/插件/UI组件逻辑唯一键与原主键冲突。目标应用/页面仍0，尚未还原；不得把之前定向测试通过视作跨环境迁移可用。源数据源实例为0。后续需定义身份映射与缺表准备，不绕过校验。证据 tmp/test-governance/backup-restore-beta/result.md。原目标分支9个改动按用户确认保存在命名stash，未丢弃。

2026-09-22 12 用户进一步确认“把系统部分导出作为模板”的独立场景：只处理主数据源，选择页面模板、应用已发布接口与数据建模定义；导入分配新ID并重绑引用、缺自定义表由原owner自动创建，冲突不覆盖。不带业务行/插件包/源用户凭证；插件仅逻辑依赖，目标自行安装校验。该模板入口与原选择性备份分开。beta真实还原已通过：80页面/分组、3发布应用、6自定义表，包3.68MiB，发布接口200、代表性页面浏览器渲染、物理字段齐全且0业务行；5published模型可查，1draft保持未发布。用户目标已从此前“原身份结构备份跨环境失败”推进为可移植模板成功；验收入口 tmp/test-governance/portable-template/result.md。无新增待批准范围或截止日期。
